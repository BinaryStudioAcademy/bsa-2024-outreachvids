import { type RemotionAvatarScene } from './remotion-avatar-scene.js';

type InputProperties = {
    scenes: RemotionAvatarScene[];
    totalDurationInFrames: number;
};

export { type InputProperties as InputProps };
